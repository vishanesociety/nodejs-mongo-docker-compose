NODE_ENV=development
APP_PORT=8080

# Local development (used with docker compose up)
DATABASE_URL=mongodb://mongodb:27017/BOOKS

# Production (Railway) - format with authSource=admin required
# DATABASE_URL=mongodb://<user>:<password>@<host>:<port>/<dbname>?authSource=admin
